using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

// Represents an action menu, composed of 2 to 4 buttons (actions)
namespace GameView
{
	public class ActionMenu : MonoBehaviour
	{
		[SerializeField] private Button[] m_actions;

		// Use this for initialization
		private void Start()
		{

		}

		// Update is called once per frame
		private void Update()
		{

		}
	}
}